#include <stdio.h>

int main()
{
    // Squer area
    // int length,breath;
    // printf("Enter length: ");
    // scanf("%d",&length);
    // printf("Enter breath: ");
    // scanf("%d",&breath);
    // int area=length*breath;
    // printf("Area is: %d\n",area);

    // Cirule
    float pi = 3.14;
    int radius = 5;
    printf("The area of circule is: %f\n", pi * radius * radius);
    float height = 7.8;
    printf("The area of circule is: %f\n", pi * radius * radius * height);

    return 0;
}