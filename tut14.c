#include <stdio.h>

int main()
{
    int i = 0;
    // printf("Enter a number: ");
    // scanf("%d",&num);
    while (i < 54)
    {
        printf("%d\n", i + 1);
        i = i + 1;
    }

    return 0;
}